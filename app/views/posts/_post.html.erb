<%= link_to user_post_path(@user, id: post..id) %>

    <p class="post"> Post #<%= post.id %></p>
    <p><%= post.Text %></p>
    <p> 
        <span>
            Comments: <%= post.comments_count || 0 %>
            Likes: <%= post.likes_counter || 0 %>
        </span>
    </p>
 
<% end %>